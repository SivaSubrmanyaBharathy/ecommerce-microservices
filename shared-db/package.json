{
  "name": "shared-db",
  "version": "1.0.0",
  "description": "Shared database configuration and migrations using Knex",
  "main": "knexfile.js",
  "scripts": {
    "migrate": "knex migrate:latest",
    "migrate:rollback": "knex migrate:rollback"
  },
  "dependencies": {
    "fastify-plugin": "^6.0.0",
    "knex": "^3.1.0",
    "pg": "^8.11.3"
  }
}
